#pragma once

namespace sceneUtil {
	void MakeCoordGizmo(
		const Vec3 & origin,
		const Vec3 & fDir,
		const Vec3 rDir,
		const Vec3 & uDir,
		const float scale,
		void * bodies
	);
} // namespace scene util